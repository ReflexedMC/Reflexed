package mc.reflexed.util;

import mc.reflexed.Reflexed;
import org.bukkit.Server;
import org.bukkit.plugin.Plugin;

public class Util {
    protected final static Plugin plugin = Reflexed.get();
    protected final static Server server = plugin.getServer();
}
